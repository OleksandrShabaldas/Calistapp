<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Share cards are rendered to PNGs here, then handed to other apps via the FileProvider. -->
    <cache-path name="shared" path="shared/" />
</paths>
