<resources>
    <!-- Splash background (the adaptive icon draws its own warm-onyx gradient). Near-black to match
         the app's onyx UI, so there's no colour flash into the dark Compose screens. -->
    <color name="ic_launcher_background">#0B0A0C</color>
    <!-- Matches theme.Ink so there's no colour flash between splash and the Compose UI. -->
    <color name="window_background">#07090D</color>
</resources>
